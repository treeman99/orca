// Class strings shared by the AI Vault header controls, which live in two files so that
// neither exceeds the tsx line budget.

export const VAULT_HEADER_CONTROL_CLASS = 'size-6 shrink-0'

export const AGENT_BULK_ACTION_CLASS =
  'rounded-full px-2 py-0.5 text-[11px] font-normal text-muted-foreground focus:text-foreground'
